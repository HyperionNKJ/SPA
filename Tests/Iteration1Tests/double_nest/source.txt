procedure P {
	read x;						// 1 
	if (b < 15) then {			// 2
		while (c <= x) {		// 3
			x = x + a;			// 4
			c = 16;				// 5
		}
	}
	else {
		while (b >= c) {		// 6
			c = 7 + a - b;		// 7
			a = b + c;			// 8
		}
	}
	while (a > c) {				// 9
		if (b <= c) then {		// 10
			b = b + a			// 11
		}
		else {
			x = x + b			// 12
			c = x + c			// 13
		}
		a = a - 1				// 14
	}
	x = c;						// 15
	k = x + a - b + a + a;		// 16 
	print k;					// 17
}